# What was tested

Regenerated by `tools/make_tables.py` from the keys of the files in `results/`. Nothing here is transcribed by hand; the only hand-written part is the English for names that were written in Chinese during development, and the script stops if it meets a name it has no English for.

`07_verdict_counts.md` says how each comparison came out. This page says what was compared.

## The eleven sets of pretrained weights

These are an axis inside most families, not a family of their own. That is why a row in the README covering three families can hold several hundred comparisons: each family crosses these eleven with something else.

| Weights | Architecture | Corpus | Pretraining |
|---|---|---|---|
| RN50-gastro5M | ResNet-50 | GastroNet-5M | DINOv1 |
| RN50-1M | ResNet-50 | 1M subset | DINOv1 |
| RN50-200K | ResNet-50 | 200K subset | DINOv1 |
| RN50-SWSL | ResNet-50 | GastroNet-5M | billion-scale semi-supervised init, then DINOv1 |
| RN50-MOCOv2 | ResNet-50 | in domain | MoCo v2 |
| RN50-SIMCLRv2 | ResNet-50 | in domain | SimCLR v2 |
| ViTS-gastro | ViT-S/16 | in domain | DINOv1 |
| DINOv2-ViT-B | ViT-B | in domain | DINOv2 |
| RN50-in1k-sup | ResNet-50 | ImageNet-1k | supervised |
| RN50-in1k-dino | ResNet-50 | ImageNet-1k | DINO |
| ViTS-in1k-dino | ViT-S/16 | ImageNet-1k | DINO |

The first eight are the provider's listing; the last three are out-of-domain baselines carried for contrast. Provenance is in `deliver`-side notes and in section 3 of the report.

## The choice of pretrained weights, and what it interacts with

### All backbones, A-class components

eleven weight sets x the components that had passed an earlier grid. 616 comparisons, from `results/d1_all_backbones.json`.

| Axis | Levels tested |
|---|---|
| module (1) | `op` |
| backbone (12) | `DINOv2-ViT-B`, `RN50-1M`, `RN50-200K`, `RN50-MOCOv2`, `RN50-SIMCLRv2`, `RN50-SWSL`, `RN50-gastro5M`, `RN50-in1k-dino`, `RN50-in1k-sup`, `ViTS-gastro`, `ViTS-in1k-dino`, `fov` |
| shrinkage (15) | `0.1`, `0.2`, `0.4`, `0.6`, `DINOv2-ViT-B`, `RN50-1M`, `RN50-200K`, `RN50-MOCOv2`, `RN50-SIMCLRv2`, `RN50-SWSL`, `RN50-gastro5M`, `RN50-in1k-dino`, `RN50-in1k-sup`, `ViTS-gastro`, `ViTS-in1k-dino` |
| component (23) | `0.1`, `0.2`, `0.4`, `0.6`, `LSE β=1.0`, `LSE β=2.0`, `LSE β=3.9`, `LSE β=8.0`, `global average pooling`, `softattn T=1.0`, `softattn T=2.0`, `softattn T=4.0`, `top 1 position`, `top 10% of positions (k = 20)`, `top 10% of positions (k = 26)`, `top 10% of positions (k = 5)`, `top 2% of positions (k = 1)`, `top 2% of positions (k = 4)`, `top 2% of positions (k = 5)`, `top 5% of positions (k = 10)`, `top 5% of positions (k = 13)`, `top 5% of positions (k = 2)`, `Σp²/Σp` |

### Backbone x module interaction

backbone x field-of-view mask, two-stage resize, pooling operator. 48 comparisons, from `results/b1_backbone_module.json`.

| Axis | Levels tested |
|---|---|
| backbone (4) | `DINOv2-ViT-B`, `RN50-5M-DINOv1`, `RN50-PCA384`, `ViT-S-5M-DINOv1` |
| pooling or module (3) | `GAP`, `top1`, `top2pct` |
| shrinkage (4) | `0.1`, `0.2`, `0.4`, `0.6` |

### Multi-backbone fusion

score-level fusion across backbones. 10 comparisons, from `results/r7_bbfusion.json`.

| Axis | Levels tested |
|---|---|
| candidate pool (5) | `S1: three in-domain CNNs of comparable strength`, `S2: six in-domain CNNs`, `S3: eight CNNs including out-of-domain`, `S4: three backbones across architectures`, `S5: all eleven` |
| combination rule (2) | `rank`, `z` |

## How features come off the network

### Read-out basis x component

read-out basis x power transform x shrinkage x fit scope x pooling. 474 comparisons, from `results/e3_basis_component.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |
| read-out basis (8) | `A1: the layer-4 spatial map, 7x7`, `A3: the layer-4 map, globally averaged`, `B1: last-layer patch grid`, `B2: last two blocks, patches concatenated`, `B3: class token broadcast into every patch`, `B4: last-layer class token alone`, `B5: last four blocks, class tokens concatenated`, `B6: class token with the patch mean` |
| component, part 1 (2) | `power transform off`, `power transform on (alpha = 0.5)` |
| component, part 2 (3) | `shrinkage 0.02`, `shrinkage 0.10`, `shrinkage 0.35` |
| component, part 3 (2) | `fit scope 0.25`, `fit scope 1.00` |
| component, part 4 (3) | `global average`, `maximum`, `top 2% of positions` |

### Layer 3+4 replication

the same component menu, on layer 3+4 instead of layer 4. 192 comparisons, from `results/e3b_cnn_layer.json`.

| Axis | Levels tested |
|---|---|
| backbone (8) | 8 of the eleven: `RN50-1M`, `RN50-200K`, `RN50-MOCOv2`, `RN50-SIMCLRv2`, `RN50-SWSL`, `RN50-gastro5M`, `RN50-in1k-dino`, `RN50-in1k-sup` |
| read-out basis (1) | `A2: layers 3 and 4 together` |
| component, part 1 (2) | `power transform off`, `power transform on (alpha = 0.5)` |
| component, part 2 (3) | `shrinkage 0.02`, `shrinkage 0.10`, `shrinkage 0.35` |
| component, part 3 (2) | `fit scope 0.25`, `fit scope 1.00` |
| component, part 4 (3) | `global average`, `maximum`, `top 2% of positions` |

### Layer choice

which layer the features are taken from. 11 comparisons, from `results/r4_layer.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |

## How the head is built

### Same-backbone head fusion

fusing two heads on one backbone, weight swept 0.50 to 1.00. 99 comparisons, from `results/r6_fusion.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |
| fusion weight (9) | `weight 0.50`, `weight 0.60`, `weight 0.70`, `weight 0.80`, `weight 0.85`, `weight 0.90`, `weight 0.95`, `weight 0.98`, `weight 1.00` |

### One-class vs discriminant

modeling the normal class alone instead of a discriminant. 44 comparisons, from `results/q1_oneclass_all.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |
| shrinkage (4) | `0.1`, `0.2`, `0.4`, `0.6` |

### Geometric read-outs

kNN density ratio, Ledoit-Wolf and OAS shrinkage, random subspace. 32 comparisons, from `results/q3_geometry_all.json`.

| Axis | Levels tested |
|---|---|
| backbone (8) | 8 of the eleven: `RN50-1M`, `RN50-200K`, `RN50-MOCOv2`, `RN50-SIMCLRv2`, `RN50-SWSL`, `RN50-gastro5M`, `RN50-in1k-dino`, `RN50-in1k-sup` |
| method (4) | `Ledoit-Wolf shrinkage`, `k-nearest-neighbor density ratio, k = 20`, `oracle-approximating shrinkage`, `random subspace, 25% of dimensions, 20 draws` |

### External covariance target

an external covariance target, shrinkage swept 0.02 to 0.90. 27 comparisons, from `results/r2_extcov.json`.

| Axis | Levels tested |
|---|---|
| covariance target (27) | `diagnostic: Σ_ext, shrinkage 0.02`, `diagnostic: Σ_ext, shrinkage 0.05`, `diagnostic: Σ_ext, shrinkage 0.10`, `diagnostic: Σ_ext, shrinkage 0.20`, `diagnostic: Σ_ext, shrinkage 0.35`, `diagnostic: Σ_ext, shrinkage 0.50`, `diagnostic: Σ_ext, shrinkage 0.70`, `diagnostic: Σ_ext, shrinkage 0.90`, `diagnostic: τI, shrinkage 0.02`, `diagnostic: τI, shrinkage 0.05`, `diagnostic: τI, shrinkage 0.10`, `diagnostic: τI, shrinkage 0.20`, `diagnostic: τI, shrinkage 0.35`, `diagnostic: τI, shrinkage 0.50`, `diagnostic: τI, shrinkage 0.70`, `diagnostic: τI, shrinkage 0.90`, `external covariance only, EVC esophagus domain, 100 images, no shrinkage`, `external covariance only, esophagus only, no shrinkage`, `external covariance only, unfiltered, no shrinkage`, `target: diagonal external covariance, EVC esophagus domain, 100 images`, `target: diagonal external covariance, esophagus only`, `target: diagonal external covariance, unfiltered`, `target: diagonal of the in-domain covariance`, `target: external covariance, EVC esophagus domain, 100 images`, `target: external covariance, esophagus only`, `target: external covariance, unfiltered`, `target: scaled identity (delivered)` |

### Fit-scope arm D

what data the head is fitted on. 16 comparisons, from `results/c4_arm_d.json`.

| Axis | Levels tested |
|---|---|
| backbone (4) | `DINOv2-ViT-B`, `RN50-SIMCLRv2`, `RN50-gastro`, `ViTS-gastro` |
| shrinkage (4) | `0.1`, `0.2`, `0.4`, `0.6` |

## How 49 positions become one score

### Instance selection (MIL)

top-q instance selection, q = 0.50 / 0.25 / 0.10. 33 comparisons, from `results/r5_mil.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |
| selected fraction (3) | `top 0.10 of positions`, `top 0.25 of positions`, `top 0.50 of positions` |

## Preprocessing and invariance

### Normalization family

per-dimension z, L2, per-position z, none, crossed with shrinkage. 176 comparisons, from `results/d2_norm_family.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |
| shrinkage (4) | `0.1`, `0.2`, `0.4`, `0.6` |
| feature normalization (4) | `l2`, `none`, `perdim_z`, `perpos_z` |

### TTA and color normalization

horizontal-flip TTA and white-patch color normalization. 30 comparisons, from `results/r8_tta_color.json`.

| Axis | Levels tested |
|---|---|
| backbone (10) | 10 of the eleven: `DINOv2-ViT-B`, `RN50-1M`, `RN50-200K`, `RN50-MOCOv2`, `RN50-SIMCLRv2`, `RN50-gastro5M`, `RN50-in1k-dino`, `RN50-in1k-sup`, `ViTS-gastro`, `ViTS-in1k-dino` |
| method (3) | `horizontal-flip TTA, averaged`, `horizontal-flip TTA, replaced`, `white-patch color normalization` |

### Field-of-view mask

masking the endoscope's circular field of view. 16 comparisons, from `results/c2_fov_mask.json`.

| Axis | Levels tested |
|---|---|
| backbone (4) | `DINOv2-ViT-B`, `RN50-SIMCLRv2`, `RN50-gastro`, `ViTS-gastro` |
| shrinkage (4) | `0.1`, `0.2`, `0.4`, `0.6` |

## How the configuration is assembled

### Greedy combination search

building the configuration one component at a time. 11 comparisons, from `results/r11_combo.json`.

| Axis | Levels tested |
|---|---|
| backbone (11) | the 11 sets above |

