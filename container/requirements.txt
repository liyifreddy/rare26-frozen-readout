# All versions pinned. timm is the one dependency that both drifts over time and
# directly determines whether load_state_dict(strict=...) accepts the checkpoint.
# This combination is the one in the container that ran on the RARE26 platform.
torch==2.7.1
timm==1.0.19
numpy==2.2.6
Pillow==11.3.0
SimpleITK==2.5.0
